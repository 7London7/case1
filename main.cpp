#include <iostream>
#include <string>
#include "case.h"
using namespace std;

int main()
{
    int ch, bit, m;
    cin >> ch >> bit >> m;
    cout << itc_probdop(ch, bit, m);
    return 0;
}
